foo:
  bar: foobar
  baz: some other stuff
steps:
  - name: ":buildkite: test"
    agents:
      queue: 'dlntest-k8s'
    plugins:
      EmbarkStudios/k8s:
        image: alpine
        command: ["sh", "-c", "uname -a"]
        git-credentials-secret-key: git-credentials
        git-ssh-secret-key: git-ssh-key
        environment:
          - FOO=bar
          - BAZ=tjosan hejsan
